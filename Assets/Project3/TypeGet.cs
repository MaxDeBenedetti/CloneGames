using UnityEngine;
using System.Collections;

public class TypeGet : MonoBehaviour {

	// Use this for initialization
	void Start () {
		print(this.gameObject.GetType());
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
